/**
 * Home Page Tests
 *
 * TODO: Implement tests for the home page (https://automationexercise.com).
 *
 * Suggested test cases to write:
 *   - Page loads with the correct title ("Automation Exercise")
 *   - Featured products are displayed (count > 0)
 *   - Category sidebar is visible
 *   - Brands section is visible
 *   - Expanding a category (e.g. Women) shows subcategories
 *   - Clicking a category navigates to the correct URL
 *   - Clicking a brand navigates to the brand products page
 *   - Adding a product to cart shows the cart modal
 *   - "Continue Shopping" button closes the modal
 *   - "View Cart" link in modal navigates to /view_cart
 *   - Subscribing with a valid email shows a success message
 *
 * HINT: Import and use the HomePage page object.
 *   const HomePage = require('../pages/HomePage');
 */

const { test, expect } = require('@playwright/test');

test.describe('Home Page', () => {
  // TODO: Add beforeEach to navigate to the home page

  // TODO: Write your tests here
});
