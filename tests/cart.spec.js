/**
 * Cart Tests
 *
 * TODO: Implement tests for the cart page (https://automationexercise.com/view_cart).
 *
 * Suggested test cases:
 *   - Cart page loads at /view_cart
 *   - Adding a product from the home page appears in the cart
 *   - Cart item displays a name
 *   - Cart item displays a price containing "Rs."
 *   - Cart item quantity defaults to 1
 *   - Adding a product with quantity 2 from the detail page shows qty 2 in cart
 *   - Removing an item decreases the cart count
 *   - Adding two different products results in 2 cart rows
 *   - "Proceed to Checkout" button is visible when cart has items
 *
 * HINT: Import and use the CartPage and HomePage page objects.
 *   const CartPage = require('../pages/CartPage');
 *   const HomePage = require('../pages/HomePage');
 */

const { test, expect } = require('@playwright/test');

test.describe('Cart', () => {
  // TODO: Write your tests here
});
